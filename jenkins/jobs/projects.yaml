- project:
    name: sandbox
    github-org: openstack-dev
    node:
      - ubuntu-xenial
      - centos-7
    vendor: cisco

    jobs:
        - 'noop-check-communication-{node}'
        - 'gate-{name}-dsvm-tempest-full-{node}'

- project:
    name: networking-cisco
    github-org: openstack
    node:
      - ubuntu-xenial
      - centos-7
    vendor: cisco

    jobs:
        - 'gate-{name}-dsvm-tempest-full-{node}'
        - 'gate-{name}-dsvm-tempest-full-nexus-{node}'
