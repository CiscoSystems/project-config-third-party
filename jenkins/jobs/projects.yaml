- project:
    name: sandbox
    github-org: openstack-dev
    node: ubuntu-xenial
    vendor: cisco

    jobs:
        - noop-check-communication
        - 'gate-{name}-dsvm-tempest-full'

- project:
    name: networking-cisco
    github-org: openstack
    node: ubuntu-xenial
    vendor: cisco

    jobs:
        - 'gate-{name}-dsvm-tempest-full'
