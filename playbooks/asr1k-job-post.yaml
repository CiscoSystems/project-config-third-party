---
- hosts: primary
  vars:
    nexus_interfaces: >
      {{ ci_node_interfaces }} +
      {{ routers|map('extract', connections, ['data', 'port'])|list }}
  roles:
    - role: clear-nexus-vlans
      ignore_errors: true
    - release-claimed-vlans

- hosts: all
  roles:
    - tear-l2-tunnels
